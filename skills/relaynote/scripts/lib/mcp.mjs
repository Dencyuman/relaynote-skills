import { accessToken, AuthError } from './auth.mjs';
let nextId=0;
async function rpc(method,params,timeoutMs=35000){const {base,token}=await accessToken();const id=++nextId;const r=await fetch(base+'/mcp',{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json',Accept:'application/json, text/event-stream'},body:JSON.stringify({jsonrpc:'2.0',id,method,params}),signal:AbortSignal.timeout(timeoutMs),redirect:'error'});if(r.status===401||r.status===403)throw new AuthError('Access denied. Reconnect from login or Settings.');if(!r.ok)throw new Error(`MCP request failed (${r.status})`);
 if(r.headers.get('content-type')?.includes('application/json'))return r.json();
 const reader=r.body.getReader(),decoder=new TextDecoder();let buf='';try{while(true){const {done,value}=await reader.read();if(done)throw new Error('MCP stream ended without a result');buf+=decoder.decode(value,{stream:true});let end;while((end=buf.indexOf('\n\n'))>=0){const frame=buf.slice(0,end);buf=buf.slice(end+2);const data=frame.split('\n').filter(l=>l.startsWith('data:')).map(l=>l.slice(5).trim()).join('\n');if(data){const m=JSON.parse(data);if(m.id===id)return m}}if(buf.length>4000000)throw new Error('MCP response too large')}}finally{await reader.cancel().catch(()=>{})}}
export async function tool(name,args){const message=await rpc('tools/call',{name,arguments:args});if(message.error)throw new Error('MCP protocol error');if(message.result?.isError)throw new Error(message.result.content?.find(c=>c.type==='text')?.text??'MCP tool failed');if(!message.result?.structuredContent)throw new Error('Missing structured result');return message.result.structuredContent;}
export const getReview = session_id => tool('get_session_review',{session_id});
export async function waitReview(session_id,round){const r=await tool('wait_for_review',{session_id,timeout_seconds:25});if(r.current_round!==round)throw new Error('ROUND_CHANGED');return r.latest_review?.round===round?r:null;}
